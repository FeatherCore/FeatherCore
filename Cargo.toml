[workspace]
name = "feathercore"
description = "FeatherCore Build System and RTOS Framework"

members = [
    "common/arch/arm",
    "common/arch/riscv",
    "common",
    "boot",
    "kernel",
]

[profile.dev]
opt-level = 1

[profile.release]
opt-level = 3
lto = true
codegen-units = 1

