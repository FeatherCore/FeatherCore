# Tools

Tooling is split by ownership and reuse scope:

- `firmware/`: reusable Feather firmware build and image packaging scripts.
- `vendor/`: vendor-specific local tool caches and setup notes.

Do not commit proprietary vendor binaries unless their license explicitly allows
redistribution.
