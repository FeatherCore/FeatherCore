# Vendor Tools

This directory is reserved for vendor-specific local tool caches.

Common Feather build and packaging scripts should stay under `tools/firmware`
or another common `tools` subdirectory.  Proprietary vendor binaries should be
kept under a vendor namespace here and ignored by Git unless their license
explicitly allows redistribution.
